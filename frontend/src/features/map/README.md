# Map Feature

Map-specific page composition, feature-level state, and map workflows belong here. Existing map components currently live in `src/components/map` and can be moved here when the team introduces route-level feature organization.

