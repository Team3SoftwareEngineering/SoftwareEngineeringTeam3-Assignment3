# Enforce consistent line endings across environments (Ubuntu/Linux default).
* text=auto eol=lf

# Keep Windows command scripts in CRLF for native execution.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Explicit LF for Unix shell scripts.
*.sh text eol=lf
